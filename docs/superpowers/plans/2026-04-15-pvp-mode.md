# PvP Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通双人对战房间创建、第二玩家加入、状态同步与胜负收尾。

**Architecture:** 继续沿用当前 Socket.IO server-authoritative 架构，在 server 增加房间匹配与双 socket 生命周期管理，在 client lobby 增加真实 PvP 入口。

**Tech Stack:** TypeScript, Socket.IO, React, Zustand, Vitest

---

### Task 1: 服务端房间管理

**Files:**
- Modify: `packages/server/src/gameManager.ts`
- Modify: `packages/server/src/socketHandler.ts`

- [ ] 增加等待中的 PvP 房间或显式创建/加入逻辑。
- [ ] 允许第二玩家加入并映射到 player index 1。

### Task 2: 客户端 PvP 入口

**Files:**
- Modify: `packages/client/src/components/lobby/Lobby.tsx`
- Modify: `packages/client/src/stores/gameStore.ts`

- [ ] 打开 PvP 按钮。
- [ ] 让 lobby 能发起/加入 PvP 匹配并正确进入 hero-select / game board。

### Task 3: 测试与联调

**Files:**
- Create/Modify: `packages/server/test/socketHandler.test.ts`
- Create/Modify: `packages/client/src/hooks/useGameSocket.test.tsx`

- [ ] 增加双玩家加入与广播测试。
- [ ] 验证对局结束和断线处理。
